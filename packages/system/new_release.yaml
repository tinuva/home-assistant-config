---
#automation:
#  - alias: Update notification
#    trigger:
#      - platform: state
#        entity_id: binary_sensor.updater
#        to: 'on'
#    action:
      # - service: script.alert
      #   data:
      #     target: 'alerts-david'
      #     icon: ':rotating_light: '
      #     object: 'Home Assistant'
      #     message: "New Home Assistant  {{ state_attr('binary_sensor.updater', 'newest_version') }}  is now available."
